What did I learn today?
I learned that tuples are immutable and it goes between parentheses. The difference between remove() and discard() is how they handle values that are not present in a set. If the value does not exist, discard() does nothing, while remove() raises a KeyError. 
Sets are useful for removing duplicate values.

What was difficult?
The mini challenge.

What mistakes did I make?
I mistankenly used curly braces to remove duplicates in a list, and that resulted in some errors

What did I learn from those mistakes?
Sets can be used as methods, but that will return you a set, so if you want a list in return, you have to make the conversion.