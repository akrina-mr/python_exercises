1. What advantage do functions have?
Functions allow us to reuse code and avoid repeating logic. They also make programs easier to maintain.

2. What is the difference between print() and return}
print show us information.
return, returns us a result that function calls
when you use a return in your function and you want to see the exit, you must to put it inside a print, otherwise you won't see the result.
While with print inside your function, you just have to call it with a value.

3. What was the most difficult part of the day?
multiplication exercise, because I didn't know how to use print and return

4. which exercise took you the longest?
multiplication exercise, because I was making some tests

5. How would you use functions in a data project? 
I can use it with values and variables than can change in time, to identify easily the errors and change the variables quickly.
But to be honest, I would like to know how can I use functions in my rel_client_identifiers project